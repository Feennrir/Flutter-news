import 'package:flutter/material.dart';


// Background color
const colorBackground = Color.fromRGBO(19, 19, 22, 1.0);
const colorAccentuation = Color.fromRGBO(36, 37, 41, 1.0);

// Text colors
const colorText = Colors.white;
const colorTextAccentuation = Color.fromRGBO(155, 159, 154, 1.0);

const marginContainer = EdgeInsets.only(left: 15, right: 15, top: 10, bottom: 10);
const borderRadius = BorderRadius.all(Radius.circular(10));

// Font sizes
const xl35 = 35.0;
const lg20 = 20.0;
const md16 = 16.0;
const sm14 = 14.0;
const tiny12 = 12.0;
const tiny10 = 10.0;